// Test-only stub. `server-only` is a compile-time guard bundled inside Next;
// it is not installable as a standalone package, so scripts run under tsx map
// the bare `server-only` import here (see scripts/tsconfig.scripts.json).
export {};
